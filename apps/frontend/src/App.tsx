import { AddPostForm, PostsList } from './features';

const App = () => {
    return (
        <main className='app-container'>
            <AddPostForm />
            <PostsList />
        </main>
    );
};
export default App;
